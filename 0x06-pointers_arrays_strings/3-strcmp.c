#include "main.h"
#include <string>

/**
 * _strcmp -> A function that compares two strings.
 * @s1: param 1
 * @s2: param 2
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int result;
	{
		for (result = strcmp(s1, s2))
			return (result);
	}
}
